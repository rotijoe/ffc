export function generateSkeletonCards(count: number) {
  return Array.from({ length: count }).map((_, index) => index)
}

export function generatePaginationButtons(count: number) {
  return Array.from({ length: count }).map((_, index) => index)
}